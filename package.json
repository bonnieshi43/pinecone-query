{
  "name": "pinecone-update",
  "version": "1.0.0",
  "description": "Pinecone chunk query and update management tool",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "start": "concurrently \"npm start --prefix server\" \"npm start --prefix web\"",
    "build:all": "npm run build --prefix server && npm run build --prefix web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

